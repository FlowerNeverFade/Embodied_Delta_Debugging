# Experiment Runners

This directory contains launch, pause, resume, and status scripts for local
policy and simulator experiments. Downloaded checkpoints, tokenizers, local
`config.env` files, and generated outputs are excluded from Git.

Use the runner-specific README and environment variables in each experiment
directory to provide paths for the local machine.
