# pi05_libero-libero_10

Independent run folder for `pi05_libero` on `libero_10`.

Outputs stay under `./outputs`; this folder uses policy port `8010`.

```bash
./run_smoke.sh
./start_background.sh
./status.sh
./stop.sh
```
